// this is declaration file for another library
// if we want to have different js file included together with this declaration file, we need to create intermediate other-lib.js script

export function testFn(x: number, y: number): number